{
  "name": "foolorddotcom",
  "private": true,
  "scripts": {
    "build": "node index.js",
    "deploy": "npm run build && surge --project build",
    "watch": "nodemon --ext js,scss,md,html --watch src --watch layouts --exec \"npm run build\""
  },
  "dependencies": {
    "handlebars": "^4.0.5",
    "metalsmith": "^2.1.0",
    "metalsmith-layouts": "^1.4.1",
    "metalsmith-markdown": "^0.2.1",
    "metalsmith-permalinks": "^0.5.0",
    "metalsmith-sass": "^1.3.0",
    "nodemon": "^1.9.2"
  }
}
